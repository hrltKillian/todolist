{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <a href="{{ path('app_tasks_show', {userSlug: userSlug}) }}">Retour</a>
    {{ form(form) }}
{% endblock %}